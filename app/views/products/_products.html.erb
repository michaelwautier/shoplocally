<%= link_to  icon('fas', 'plus', 'Add a product'), new_shop_product_path(@shop) %>

<div class="row mt-4">
  <% products.each do |product| %>
    <div class="col-12 col-md-6 col-xl-4">
      <%= render 'products/product_card', product: product %>
    </div>
  <% end %>
</div>
